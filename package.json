{
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w @rate-limiter/shared && npm run build -w rate-limiter-backend && npm run build -w rate-limiter-frontend",
    "test": "npm run test -w @rate-limiter/shared && npm run test -w rate-limiter-backend"
  }
}
